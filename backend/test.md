# Руководство по тестированию FastAPI Shop API в Postman

## Подготовка к тестированию

### 1. Запуск приложения

Перед началом тестирования убедитесь, что:

```bash
# 1. Установлены зависимости
pip install -r backend/requirements.txt

# 2. База данных заполнена тестовыми данными
python backend/seed_db.py

# 3. Сервер запущен
python backend/run.py
```

Сервер должен быть доступен по адресу: `http://localhost:8000`

### 2. Настройка Postman

1. Создайте новую коллекцию "FastAPI Shop API"
2. Создайте переменную окружения:
   - Variable: `base_url`
   - Initial Value: `http://localhost:8000`
   - Current Value: `http://localhost:8000`

---

## Тестирование эндпоинтов

### 📁 **1. Root Endpoint**

**Проверка работоспособности API**

- **Method:** `GET`
- **URL:** `{{base_url}}/`
- **Ожидаемый результат:**
```json
{
    "message": "Welcome to fastapi shop API",
    "docs": "api/docs"
}
```

---

### 📂 **2. Categories (Категории)**

#### 2.1. Получить все категории

- **Method:** `GET`
- **URL:** `{{base_url}}/api/categories/`
- **Ожидаемый результат:** Список всех категорий
```json
[
    {
        "name": "Electronics",
        "slug": "electronics",
        "id": 1
    },
    {
        "name": "Clothing",
        "slug": "clothing",
        "id": 2
    }
]
```

#### 2.2. Получить категорию по ID

- **Method:** `GET`
- **URL:** `{{base_url}}/api/categories/1`
- **Ожидаемый результат:**
```json
{
    "name": "Electronics",
    "slug": "electronics",
    "id": 1
}
```

**Тестовые случаи:**
- ✅ ID существующей категории (1, 2, 3, 4)
- ❌ ID несуществующей категории (999) - должен вернуть 404

---

### 📦 **3. Products (Товары)**

#### 3.1. Получить все товары

- **Method:** `GET`
- **URL:** `{{base_url}}/api/products/`
- **Ожидаемый результат:**
```json
{
    "products": [
        {
            "name": "Wireless Headphones",
            "description": "High-quality wireless headphones...",
            "price": 299.99,
            "category_id": 1,
            "image_url": "https://images.unsplash.com/...",
            "id": 1,
            "created_at": "2024-12-06T10:00:00Z",
            "category": {
                "name": "Electronics",
                "slug": "electronics",
                "id": 1
            }
        }
    ],
    "total": 13
}
```

#### 3.2. Получить товар по ID

- **Method:** `GET`
- **URL:** `{{base_url}}/api/products/1`
- **Ожидаемый результат:** Подробная информация о товаре

**Тестовые случаи:**
- ✅ ID существующего товара (1-13)
- ❌ ID несуществующего товара (999) - должен вернуть 404

#### 3.3. Получить товары по категории

- **Method:** `GET`
- **URL:** `{{base_url}}/api/products/category/1`
- **Ожидаемый результат:** Список товаров категории Electronics

**Тестовые случаи:**
- ✅ Category ID 1 (Electronics) - ~5 товаров
- ✅ Category ID 2 (Clothing) - ~1 товар
- ✅ Category ID 3 (Books) - ~3 товара
- ✅ Category ID 4 (Home & Garden) - ~4 товара
- ❌ Category ID 999 - должен вернуть 404

---

### 🛒 **4. Cart (Корзина)**

Корзина работает с данными в формате `Dict[int, int]`, где ключ - ID товара, значение - количество.

#### 4.1. Добавить товар в корзину

- **Method:** `POST`
- **URL:** `{{base_url}}/api/cart/add`
- **Headers:** `Content-Type: application/json`
- **Body (raw JSON):**
```json
{
    "product_id": 1,
    "quantity": 2,
    "cart": {}
}
```
- **Ожидаемый результат:**
```json
{
    "cart": {
        "1": 2
    }
}
```

**Тестовые случаи:**

1. Добавление в пустую корзину:
```json
{
    "product_id": 1,
    "quantity": 2,
    "cart": {}
}
```

2. Добавление к существующему товару:
```json
{
    "product_id": 1,
    "quantity": 1,
    "cart": {
        "1": 2
    }
}
```
Результат: `{"cart": {"1": 3}}`

3. Добавление нового товара:
```json
{
    "product_id": 2,
    "quantity": 1,
    "cart": {
        "1": 2
    }
}
```
Результат: `{"cart": {"1": 2, "2": 1}}`

4. ❌ Несуществующий товар:
```json
{
    "product_id": 999,
    "quantity": 1,
    "cart": {}
}
```
Должен вернуть 404

#### 4.2. Получить детали корзины

- **Method:** `POST`
- **URL:** `{{base_url}}/api/cart`
- **Headers:** `Content-Type: application/json`
- **Body (raw JSON):**
```json
{
    "1": 2,
    "2": 1
}
```
- **Ожидаемый результат:**
```json
{
    "items": [
        {
            "product_id": 1,
            "quantity": 2,
            "name": "Wireless Headphones",
            "price": 299.99,
            "subtotal": 599.98,
            "image_url": "https://images.unsplash.com/..."
        },
        {
            "product_id": 2,
            "quantity": 1,
            "name": "Smart Watch Pro",
            "price": 399.99,
            "subtotal": 399.99,
            "image_url": "https://images.unsplash.com/..."
        }
    ],
    "total": 1000,
    "items_count": 3
}
```

**Тестовые случаи:**

1. Пустая корзина:
```json
{}
```
Результат: `{"items": [], "total": 0.0, "items_count": 0}`

2. Один товар:
```json
{
    "1": 1
}
```

3. Несколько товаров:
```json
{
    "1": 2,
    "3": 1,
    "5": 3
}
```

#### 4.3. Обновить количество товара

- **Method:** `PUT`
- **URL:** `{{base_url}}/api/cart/update`
- **Headers:** `Content-Type: application/json`
- **Body (raw JSON):**
```json
{
    "product_id": 1,
    "quantity": 5,
    "cart": {
        "1": 2,
        "2": 1
    }
}
```
- **Ожидаемый результат:**
```json
{
    "cart": {
        "1": 5,
        "2": 1
    }
}
```

**Тестовые случаи:**

1. Увеличение количества:
```json
{
    "product_id": 1,
    "quantity": 10,
    "cart": {"1": 2}
}
```

2. Уменьшение количества:
```json
{
    "product_id": 1,
    "quantity": 1,
    "cart": {"1": 5}
}
```

3. ❌ Товар не в корзине:
```json
{
    "product_id": 3,
    "quantity": 1,
    "cart": {"1": 2}
}
```
Должен вернуть 404

#### 4.4. Удалить товар из корзины

- **Method:** `DELETE`
- **URL:** `{{base_url}}/api/cart/remove/1`
- **Headers:** `Content-Type: application/json`
- **Body (raw JSON):**
```json
{
    "cart": {
        "1": 2,
        "2": 1
    }
}
```
- **Ожидаемый результат:**
```json
{
    "cart": {
        "2": 1
    }
}
```

**Тестовые случаи:**

1. Удаление существующего товара:
   - URL: `{{base_url}}/api/cart/remove/1`
   - Body: `{"cart": {"1": 2, "2": 1}}`

2. Удаление последнего товара:
   - URL: `{{base_url}}/api/cart/remove/1`
   - Body: `{"cart": {"1": 1}}`
   - Результат: `{"cart": {}}`

3. ❌ Удаление несуществующего товара:
   - URL: `{{base_url}}/api/cart/remove/999`
   - Body: `{"cart": {"1": 2}}`
   - Должен вернуть 404

---

## 🔄 Сценарии тестирования

### Сценарий 1: Полный процесс покупки

1. Получить список категорий → `GET /api/categories/`
2. Выбрать категорию "Electronics" → `GET /api/products/category/1`
3. Посмотреть детали товара → `GET /api/products/1`
4. Добавить товар в корзину → `POST /api/cart/add`
5. Добавить еще один товар → `POST /api/cart/add`
6. Посмотреть корзину → `POST /api/cart`
7. Обновить количество → `PUT /api/cart/update`
8. Удалить товар → `DELETE /api/cart/remove/{id}`
9. Проверить итоговую корзину → `POST /api/cart`

### Сценарий 2: Проверка валидации

1. Попробовать добавить товар с quantity = 0 (должна быть ошибка)
2. Попробовать добавить несуществующий товар (404)
3. Попробовать обновить товар не в корзине (404)
4. Попробовать удалить товар не в корзине (404)

---

## 📊 Checklist тестирования

### Products
- [ ] Получение всех товаров возвращает список с полной информацией
- [ ] Получение товара по ID возвращает детали товара с категорией
- [ ] Запрос несуществующего товара возвращает 404
- [ ] Фильтрация по категории возвращает только товары этой категории
- [ ] Запрос товаров несуществующей категории возвращает 404

### Categories
- [ ] Получение всех категорий возвращает полный список
- [ ] Получение категории по ID работает корректно
- [ ] Запрос несуществующей категории возвращает 404

### Cart
- [ ] Добавление товара в пустую корзину
- [ ] Добавление того же товара увеличивает quantity
- [ ] Добавление разных товаров работает корректно
- [ ] Добавление несуществующего товара возвращает 404
- [ ] Получение деталей корзины возвращает правильные расчеты
- [ ] Обновление quantity работает корректно
- [ ] Обновление несуществующего товара возвращает 404
- [ ] Удаление товара из корзины работает
- [ ] Удаление несуществующего товара возвращает 404
- [ ] Пустая корзина возвращает нулевые значения

---

## 🐛 Известные особенности

1. **Округление цены в корзине:** Поле `total` в `CartResponse` округляется до целого числа функцией `round()`.

2. **Формат корзины:** Корзина передается как словарь `{product_id: quantity}`, а не как массив объектов.

3. **Изображения:** Используются placeholder изображения с Unsplash, которые могут иногда не загружаться.

---

## 📚 Дополнительные ресурсы

- Swagger документация: `http://localhost:8000/api/docs`
- ReDoc документация: `http://localhost:8000/api/redoc`